q_000:
  prompt: "What is port number 20 used for?"
  answer: "FTP data transfer"
  
q_001:
  prompt: "What is port number 21 used for?"
  answer: "FTP control connection"

q_002:
  prompt: "What service uses port 22?"
  answer: "SSH"
  strict: true

q_003:
  prompt: "What service uses port 23?"
  answer: "Telnet"
  strict: true

q_004:
  prompt: "What service uses port 25?"
  answer: "SMTP"
  strict: true

q_005:
  prompt: "What service uses port 53?"
  answer: "DNS"
  strict: true

q_006:
  prompt: "What is the function of port 67?"
  answer: "DHCP server"
  strict: true

q_007:
  prompt: "What is the function of port 68?"
  answer: "DHCP client"
  strict: true

q_008:
  prompt: "What service uses port 80?"
  answer: "HTTP"
  strict: true

q_009:
  prompt: "What service uses port 110?"
  answer: "POP3"
  strict: true

q_010:
  prompt: "What service uses port 143?"
  answer: "IMAP"
  strict: true

q_011:
  prompt: "What service uses port 137?"
  answer: "NetBIOS-NS"
  strict: true

q_012:
  prompt: "What service uses port 138?"
  answer: "NetBIOS-DGM"
  strict: true

q_013:
  prompt: "What service uses port 139?"
  answer: "NetBIOS-SSN"
  strict: true

q_014:
  prompt: "What service uses port 389?"
  answer: "LDAP"
  strict: true

q_015:
  prompt: "What service uses port 443?"
  answer: "HTTPS"
  strict: true

q_016:
  prompt: "What service uses port 445?"
  answer: "SMB - Windows file sharing"

q_017:
  prompt: "What service uses port 636?"
  answer: "LDAPS"
  strict: true

q_018:
  prompt: "What service uses port 1720?"
  answer: "H.323"
  strict: true

q_019:
  prompt: "What service uses port 3389?"
  answer: "RDP"
  strict: true

q_020:
  prompt: "What service uses port 5060?"
  answer: "SIP - VoIP signaling (unencrypted)"

q_021:
  prompt: "What service uses port 5061?"
  answer: "SIPS - VoIP signaling (encrypted)"
