# Protocols.yaml
# Quiz items for the topic "Protocols" in Network+ (CompTIA A+)

q_000:
  prompt: "What protocol is used to translate domain names into IP addresses?"
  answer: "DNS"
  strict: true

q_001:
  prompt: "Which protocol provides secure web browsing by encrypting HTTP traffic?"
  answer: "HTTPS"
  strict: true

q_002:
  prompt: "What protocol is used to securely transfer files over SSH?"
  answer: "SFTP"
  strict: true

q_003:
  prompt: "Which protocol is used to send email from a client to a mail server?"
  answer: "SMTP"
  strict: true

q_004:
  prompt: "Name the protocol used to retrieve emails from a server using port 110."
  answer: "POP3"
  strict: true

q_005:
  prompt: "Name the protocol used to retrieve and manage emails on a server using port 143."
  answer: "IMAP"
  strict: true

q_006:
  prompt: "What protocol is used for dynamic IP address assignment?"
  answer: "DHCP"
  strict: true

q_007:
  prompt: "Which protocol provides secure remote login to a device?"
  answer: "SSH"
  strict: true

q_008:
  prompt: "What protocol allows remote management of devices using port 161?"
  answer: "SNMP"
  strict: true

q_009:
  prompt: "Which protocol provides encrypted VPN tunnels?"
  answer: "IPSec"
  strict: true

q_010:
  prompt: "What protocol is used to resolve IP addresses to MAC addresses on a local network?"
  answer: "ARP"
  strict: true

q_011:
  prompt: "Which protocol is used for file sharing on Windows networks?"
  answer: "SMB"
  strict: true

q_012:
  prompt: "What protocol initiates and terminates VoIP calls?"
  answer: "SIP"
  strict: true

q_013:
  prompt: "Which protocol allows for encrypted VoIP signaling?"
  answer: "SIPS"
  strict: true

q_014:
  prompt: "Name the protocol responsible for time synchronization across devices."
  answer: "NTP"
  strict: true

q_015:
  prompt: "What protocol is used to automatically configure network devices?"
  answer: "CDP"
  strict: true

q_016:
  prompt: "Which protocol monitors network devices and gathers statistics?"
  answer: "SNMP"
  strict: true

q_017:
  prompt: "Name the protocol that uses port 23 for unsecured remote access."
  answer: "Telnet"
  strict: true

q_018:
  prompt: "What protocol provides secure remote desktop connections?"
  answer: "RDP"
  strict: true

q_019:
  prompt: "Which protocol is used to encrypt HTTP traffic?"
  answer: "TLS"
  strict: true

q_020:
  prompt: "Name the protocol responsible for email retrieval that supports folders and multiple clients."
  answer: "IMAP"
  strict: true
