q_000:
  prompt: "What does the BSOD error code 0x0000007B indicate?"
  answer: "INACCESSIBLE_BOOT_DEVICE"
  strict: true

q_001:
  prompt: "What BSOD error code means 'PAGE_FAULT_IN_NONPAGED_AREA'?"
  answer: "0x00000050"
  strict: true

q_002:
  prompt: "What BSOD error indicates a driver issue with bad memory access?"
  answer: "DRIVER_IRQL_NOT_LESS_OR_EQUAL"
  strict: true

q_003:
  prompt: "Which BSOD error relates to a hardware failure causing memory corruption?"
  answer: "MACHINE_CHECK_EXCEPTION"
  strict: true

q_004:
  prompt: "What BSOD error code is related to a critical process dying?"
  answer: "0x000000EF"
  strict: true

q_005:
  prompt: "What does the BSOD error '0x0000001E' indicate?"
  answer: "KMODE_EXCEPTION_NOT_HANDLED"
  strict: true

q_006:
  prompt: "Which BSOD error code is often caused by incompatible or faulty drivers?"
  answer: "0x000000D1"
  strict: true

q_007:
  prompt: "What BSOD error signals a stack overflow?"
  answer: "0x000000F7"
  strict: true

q_008:
  prompt: "Which BSOD error code is associated with heap corruption?"
  answer: "0x00000016"
  strict: true

q_009:
  prompt: "What BSOD error code indicates a watchdog timeout?"
  answer: "0x00000101"
  strict: true
