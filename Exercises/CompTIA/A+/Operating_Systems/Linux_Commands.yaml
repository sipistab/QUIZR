q_000:
  prompt: "Which command lists files and directories in a folder on Linux?"
  answer: "ls"
  strict: true

q_001:
  prompt: "Which Linux command shows network interface information?"
  answer: "ifconfig"
  strict: true

q_002:
  prompt: "What Linux command displays current processes?"
  answer: "ps"
  strict: true

q_003:
  prompt: "What command in Linux is used to terminate processes?"
  answer: "kill"
  strict: true

q_004:
  prompt: "What Linux command is used to monitor real-time system resource usage?"
  answer: "top"
  strict: true

q_005:
  prompt: "What Linux command is used to find files by name?"
  answer: "find"
  strict: true

q_006:
  prompt: "How do you view the first few lines of a text file in Linux?"
  answer: "head"
  strict: true

q_007:
  prompt: "What command shows disk usage statistics in Linux?"
  answer: "df"
  strict: true

q_008:
  prompt: "What Linux command shows memory usage?"
  answer: "free"
  strict: true

q_009:
  prompt: "What Linux command displays the last logged in users?"
  answer: "last"
  strict: true

q_010:
  prompt: "What Linux command shows detailed manual pages for commands?"
  answer: "man"
  strict: true

q_011:
  prompt: "Which command changes file permissions in Linux?"
  answer: "chmod"
  strict: true

q_012:
  prompt: "Which command changes the owner of a file or directory?"
  answer: "chown"
  strict: true

q_013:
  prompt: "What command is used to copy files or directories?"
  answer: "cp"
  strict: true

q_014:
  prompt: "What command moves or renames files or directories?"
  answer: "mv"
  strict: true

q_015:
  prompt: "Which command removes files or directories?"
  answer: "rm"
  strict: true

q_016:
  prompt: "What command displays the content of a text file?"
  answer: "cat"
  strict: true

q_017:
  prompt: "Which command shows the current working directory?"
  answer: "pwd"
  strict: true

q_018:
  prompt: "What command is used to create a new directory?"
  answer: "mkdir"
  strict: true

q_019:
  prompt: "Which command removes empty directories?"
  answer: "rmdir"
  strict: true

q_020:
  prompt: "What command displays running processes in real time with user interface?"
  answer: "htop"
  strict: true

q_021:
  prompt: "Which command is used to display disk partition info?"
  answer: "fdisk"
  strict: true

q_022:
  prompt: "What command displays detailed system messages or logs?"
  answer: "dmesg"
  strict: true

q_023:
  prompt: "Which command is used to download files from the internet?"
  answer: "wget"
  strict: true

q_024:
  prompt: "What command displays or modifies network interface configurations?"
  answer: "ip"
  strict: true

q_025:
  prompt: "Which command shows free and used memory?"
  answer: "free"
  strict: true

q_026:
  prompt: "What command is used to compress files using gzip?"
  answer: "gzip"
  strict: true

q_027:
  prompt: "Which command extracts compressed files with tar?"
  answer: "tar"
  strict: true

q_028:
  prompt: "What command displays system uptime and load averages?"
  answer: "uptime"
  strict: true

q_029:
  prompt: "Which command is used to check disk usage by directory?"
  answer: "du"
  strict: true

q_030:
  prompt: "What command displays the last lines of a file in real time?"
  answer: "tail"
  strict: true
