q_000:
  prompt: "Which Windows utility is used to manage disk partitions?"
  answer: "Disk Management"
  strict: false

q_001:
  prompt: "What tool in Windows shows running processes and system performance?"
  answer: "Task Manager"
  strict: false

q_002:
  prompt: "Which Windows utility is used to check and repair disk errors?"
  answer: "chkdsk"
  strict: true

q_003:
  prompt: "What Linux command checks and repairs filesystem errors?"
  answer: "fsck"
  strict: true

q_004:
  prompt: "Which utility in Windows restores system files and settings to an earlier point?"
  answer: "System Restore"
  strict: false

q_005:
  prompt: "What Windows utility allows you to view event logs?"
  answer: "Event Viewer"
  strict: false

q_006:
  prompt: "Which Linux command displays system messages, logs, and kernel ring buffer?"
  answer: "dmesg"
  strict: true

q_007:
  prompt: "What utility in Windows can defragment and optimize drives?"
  answer: "Defragmenter"
  strict: false

q_008:
  prompt: "Which Linux command is used to manage startup services and daemons?"
  answer: "systemctl"
  strict: true

q_009:
  prompt: "What Windows tool allows you to configure startup programs and boot options?"
  answer: "MSConfig"
  strict: false

q_010:
  prompt: "Which utility can be used to wipe free space securely on Windows?"
  answer: "cipher"
  strict: true

q_011:
  prompt: "What command line tool shows disk usage statistics on Linux?"
  answer: "df"
  strict: true

q_012:
  prompt: "Which Windows utility displays detailed hardware and software configuration?"
  answer: "System Information"
  strict: false

q_013:
  prompt: "What Linux utility is used to monitor system logs continuously?"
  answer: "tail -f"
  strict: false

q_014:
  prompt: "Which Windows utility can restore deleted files from backup?"
  answer: "File History"
  strict: false

q_015:
  prompt: "What Windows utility scans and repairs Windows system files?"
  answer: "sfc"
  strict: true

q_016:
  prompt: "Which Linux command displays disk input/output statistics?"
  answer: "iostat"
  strict: true

q_017:
  prompt: "What Windows tool can diagnose and repair Windows startup problems?"
  answer: "Startup Repair"
  strict: false

q_018:
  prompt: "Which Linux command is used to schedule recurring tasks?"
  answer: "cron"
  strict: true

q_019:
  prompt: "What Windows utility manages users and groups?"
  answer: "Computer Management"
  strict: false
