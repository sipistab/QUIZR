q_000:
  prompt: "What file system is commonly used by Windows for internal hard drives?"
  answer: "NTFS"
  strict: true

q_001:
  prompt: "Which file system is commonly used on USB flash drives for compatibility across OS?"
  answer: "FAT32"
  strict: true

q_002:
  prompt: "What is the maximum file size supported by FAT32?"
  answer: "4 GB"
  strict: true

q_003:
  prompt: "Which file system is native to macOS?"
  answer: "APFS"
  strict: true

q_004:
  prompt: "What file system does Linux commonly use?"
  answer: "ext4"
  strict: true

q_005:
  prompt: "What does the acronym RAID stand for?"
  answer: "Redundant Array of Independent Disks"
  strict: true

q_006:
  prompt: "Which RAID level provides disk mirroring?"
  answer: "RAID 1"
  strict: true

q_007:
  prompt: "Which RAID level stripes data across multiple disks without redundancy?"
  answer: "RAID 0"
  strict: true

q_008:
  prompt: "Which file system supports journaling to prevent data corruption?"
  answer: "NTFS"
  strict: true

q_009:
  prompt: "What is the name of the Windows utility used to check and repair file system errors?"
  answer: "chkdsk"
  strict: true

q_010:
  prompt: "Which Linux command is used to format a partition with a specific file system?"
  answer: "mkfs"
  strict: true

q_011:
  prompt: "What does 'mount' mean in the context of file systems?"
  answer: "To make a file system accessible to the OS"
  strict: true

q_012:
  prompt: "What file system is optimized for flash storage devices?"
  answer: "exFAT"
  strict: true

q_013:
  prompt: "Which file system is often used for network file sharing in Windows?"
  answer: "SMB"
  strict: true

q_014:
  prompt: "What is the purpose of the Master Boot Record (MBR)?"
  answer: "To store partition and boot data"
  strict: true

q_015:
  prompt: "What is the GUID Partition Table (GPT)?"
  answer: "A modern partitioning scheme supporting large disks"
  strict: true

q_016:
  prompt: "What is the maximum partition size supported by MBR?"
  answer: "2 TB"
  strict: true

q_017:
  prompt: "Which file system is case-sensitive and used mainly in Unix systems?"
  answer: "ext4"
  strict: true

q_018:
  prompt: "Which Windows file system feature allows file compression?"
  answer: "NTFS"
  strict: true

q_019:
  prompt: "What is a symbolic link in file systems?"
  answer: "A shortcut pointing to a file or directory"
  strict: true

q_020:
  prompt: "What file system does Windows use for optical discs like DVDs?"
  answer: "UDF"
  strict: true
